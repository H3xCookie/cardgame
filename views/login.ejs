<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/register-login.css">
    <link rel="stylesheet" href="/css/main.css">
    
    <title>Влизане</title>
</head>
<body>
    <div id="wrapper">
    <div id="title-box" class="cool-box">
            <% if(messages.success_msg) { %>
               <p> <%= messages.success_msg %></p>
            <% } %>
    
            <% if(messages.error){ %>
                <p style="color: rgb(209, 0, 0)"><%= messages.error %></p>
            <% } %>


        <h1>ВЛИЗАНЕ</h1>
         <form action="/users/login" method="POST">
            <div>
                <input maxlength="50" ="email" name="email" id="email" placeholder="Е-поща" required>
            </div>
        
            <div>
                <input maxlength="100" type="password" name="password" id="password" placeholder="Парола" required>
            </div>

        <div>
            <input class="menu-button" type="submit" value="Влизане"/>
        </div>
        <a href="/users/register">Регистрация</a>
    </form>
    </div>
    </div>
</body>
</html>